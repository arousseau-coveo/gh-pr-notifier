version: 2
updates:
  # No language manifests in this repo (poll.mjs uses only Node built-ins, no npm deps),
  # so the only thing to keep patched is the GitHub Actions used by the CI workflows.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
